import { ReactNode } from "react"
import { WalletMultiButton } from "./WalletMultiButton"

export default function({
  children
} : {
  children?: ReactNode
}) {
  return (
    <WalletMultiButton />
  )
}